from setuptools import setup
setup(
    name='python-binary-memcached',
    version='0.0.1',
    description='A simple python rest client',
    url='https://github.com/sh3r1ll/pyrestclient',
    packages=['restclient'],
    classifiers=[
    ],
    install_requires=[
        'requests'
    ]
)
